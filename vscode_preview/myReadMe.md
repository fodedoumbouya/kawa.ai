to open code-server in a specific folder, use the following url format:
run with no authentication:
```
code-server --auth none
```
run with password authentication:
```
code-server --auth password
```